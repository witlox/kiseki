//! PyO3 bindings for the Kiseki native client.
//!
//! Enabled with `--features python`. Exposes `kiseki_read`, `kiseki_write`,
//! and `kiseki_stat` to Python workloads.
//!
//! Requires: `maturin develop --features python` to build the wheel.

// Stub: PyO3 bindings deferred until the native client API is stable.
// The feature flag and module structure are in place for when the
// native API (kiseki_read, kiseki_write, kiseki_stat) is implemented.
